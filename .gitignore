# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
.python-version

# ── Notebooks ─────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── Editors / OS ──────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── ML artifacts ──────────────────────────────────────────────────
# Checkpoints — produced by every training run, never commit
*.pth
*.pt
*.ckpt
*.safetensors
*.bin
*.npz

# Training logs and TensorBoard runs
logs/
runs/
tb_logs/
wandb/
lightning_logs/
*.log
events.out.tfevents.*

# Cached / preprocessed data
*.cache
data/
datasets/
*.tar
*.tar.gz
*.zip

# Plot outputs (regenerated by training scripts)
*.png
*.jpg
*.jpeg
*.pdf
!docs/**/*.png
!docs/**/*.jpg

# ── Reference repos cloned alongside (not part of this project) ──
# Mine-Extra-Knowledge/ has its own .git — keep as a sibling clone
# rather than committing it into this repo.
Mine-Extra-Knowledge/

# ── Misc ──────────────────────────────────────────────────────────
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.tmp

# ── Secrets — never commit ────────────────────────────────────────
.env
*.env
.env.*
.gradio/

.claude
CLAUDE.md
backbone
docs